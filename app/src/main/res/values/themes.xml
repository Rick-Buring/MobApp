<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MopApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="android:windowBackground">@color/Secondary</item>

        <item name="android:statusBarColor" tools:targetApi="l">@color/SecondaryLight</item>

        <item name="android:textColor"> @color/white</item>
    </style>
</resources>